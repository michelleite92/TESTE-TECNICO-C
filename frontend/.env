# Se usar o backend NestJS (porta 3001, prefixo /v1) — padrão
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_API_PREFIX=/v1

# Se usar o backend Express (porta 3001, prefixo /api)
# NEXT_PUBLIC_API_URL=http://localhost:3001
# NEXT_PUBLIC_API_PREFIX=/api
